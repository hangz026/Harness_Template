# 技术架构模板 (Architecture.md)

## 1. 架构目标

当前阶段只定义满足 `Spec.md` 的最小架构约束，不预铺复杂分层，也不在本文件重复当前实现效果。

约束来自：

- `Rule.md` 的硬约束
- `Spec.md` 的用户需求
- `docs/` 中的领域或接口依据
- `lessons/` 中与当前技术主题相关的实现经验

## 2. 架构职责与交付视图边界

- 本文件定义架构原则、依赖与职责边界、允许的演化方向，以及架构视图的维护规则。
- 当前已实现目录结构、模块职责、业务数据流和项目内部模块依赖统一记录在 `[交付目录]/docs/Developer_Guide.md`。
- `Developer_Guide.md` 属于可独立交付的实现说明，不得依赖 `harness/` 才能解释当前架构。
- 本文件不重复维护当前实现图，避免架构规则和实现效果形成两份事实来源。

## 3. 交付与运行边界

- 业务代码目录：`[填写]`
- 最终交付物目录：`[填写]`
- 运行时不得依赖：`[填写]`
- 运行所需源码、资源、配置和第三方资产必须进入最终交付物目录。

## 4. 当前明确边界

- `[边界 1]`
- `[边界 2]`
- `[边界 3]`

## 5. 目录与文件策略

- 当前阶段文件组织方式：`[平铺/轻量分组/已有结构]`
- 只有在以下情况出现时，才继续拆目录或拆类：
  - 单个文件职责失控
  - 同类逻辑重复
  - 核心逻辑与界面或外部接口难以分离
  - 新需求导致边界扩大

## 6. 架构呈现与维护规则

### 6.1 交付文档中的架构视图

- `Developer_Guide.md` 必须同时包含业务数据流图和项目内部模块依赖图。
- 业务数据流图表达数据或结果在业务流程中的流向，由 Agent 根据当前实现维护。
- 模块依赖图表达源码内部模块的静态依赖，由仓库工具根据源码生成。
- 两种图必须分别定义箭头含义，禁止把业务数据流误当作静态依赖。
- 自动生成范围默认只到项目内部模块级依赖，不生成完整函数调用图，也不混入第三方库。

### 6.2 更新触发条件

- 新增、删除或重命名业务模块时，必须更新业务数据流并重新生成模块依赖图。
- 改变模块依赖、职责、数据输入输出或层间调用方向时，必须重新审查两张图。
- 生成结果与代码组织不一致时，应修正源码、工具或生成规则，不得只手工修改生成结果。
- 两张图均须经 Agent 交叉检查，并交给用户审核。

## 7. 演化原则

架构按需求演化，不按想象预铺。

推荐演化顺序：

1. 先做可工作的最小数据流
2. 再抽出稳定的结果类型或帮助方法
3. 重复出现后，再拆独立类或子目录
4. 只有在验证需要时，才补充更多辅助结构

如果更简单的方案能满足当前目标，应优先选择更简单的方案。
