# 项目硬约束模板 (Rule.md)

本文档记录当前项目不可协商的硬性规则。

## 1. 交付边界

- 最终交付物目录：`[填写，例如 src/]`
- `harness/` 默认不属于最终交付物，除非本文档或 `Spec.md` 明确说明
- 业务运行不得依赖 `harness/` 中的文档、样例或共享资产原件

## 2. 外部依据

- 领域公式、接口协议、业务规则或第三方文档应放入 `docs/`，并在此列为依据
- Agent 不得猜测接口、公式或业务口径；缺失时先澄清或补充依据

## 3. 资产管理

- 共享资产来源：`[填写 shared/ 中的资产说明]`
- 共享资产复制到业务目录后，运行所需版本必须进入最终交付物目录
- 对共享资产的修改批准只对当次明确范围有效，不自动延续

## 4. 技术硬约束

- 技术栈：`[填写]`
- 目标运行环境：`[填写]`
- 必须使用的库或禁止使用的库：`[填写]`
- 关键单位、格式、协议或兼容性要求：`[填写]`

## 5. 界面与交互硬约束

- 界面元素范围以 `Spec.md` 为准
- 若某类交互被明确排除，应在此记录

## 6. 测试与验证硬约束

- 必须验证的核心路径：`[填写]`
- 可后置的验证项：`[填写]`
- 不允许跳过的验证项：`[填写]`
- 存在自动化测试时，交付目录中的 `docs/Test_Report.md` 必须以业务语言列出测试场景、关键输入、预期结果、可靠性依据和已知未覆盖范围。
- 测试报告中的数量和通过结果必须来自真实测试执行，不得手工填写或仅依据测试文件数量推断。
- 测试节点和临时测试结果文件默认不作为交付文档内容长期保存；需要技术清单时由测试框架重新收集。
- 测试通过数量不等于覆盖充分，报告必须明确当前结果能够证明和不能证明的范围。

## 7. 交付文档维护

- 若项目交付包含用户文档或开发文档，默认放在最终交付目录的 `docs/` 中
- 交付文档主体默认使用中文；控件名、类名、方法名、文件名、扩展名、专有名词与单位表达可按需要保留英文
- `User_Manual.md` 只写最终用户完成使用所需的信息，不写源码打开方式、构建环境细节或非必要实现细节；这类内容进入 `Developer_Guide.md`
- 存在自动化测试时，最小交付文档集合默认包含 `User_Manual.md`、`Developer_Guide.md` 与 `Test_Report.md`
- `Developer_Guide.md` 必须承载当前已实现结构、业务数据流图、项目内部模块依赖图和图形解释；架构原则与演化约束由 `Architecture.md` 承载
- 业务数据流图由 Agent 根据当前实现维护，项目内部模块依赖图由仓库工具从源码生成；两张图均须经 Agent 交叉检查和用户审核
- 最终交付目录中的文档不得依赖 `harness/` 才能理解当前实现，不得引用只存在于 `harness/` 的维护命令作为交付使用前提
- 当累计修改已覆盖以下类别中的两项及以上时，应提醒是否需要更新交付文档，并先提出最小文档更新方案，待批准后再更新：
  - 输入格式或输入约束
  - 用户操作流程或界面元素
  - 结果展示、单位或图表口径
  - 计算口径或算法限制
  - 交付边界或运行方式
- 即使只落在单一类别，只要该类别累计变化已经足以影响他人使用、理解或维护，也应提醒是否需要更新交付文档；可参考以下判定线索：
  - 同一类别下已发生多次连续变更
  - 已改变原有默认使用方式
  - 已改变原有输入假设或输出解释
  - 已改变原有结果判读方式
  - 已改变后续维护者需要遵守的实现口径
- 当进入交付或文档生成阶段时，必须最终核对 `User_Manual.md`、`Developer_Guide.md` 与 `Test_Report.md` 是否与当前版本一致
- 交付文档更新仍遵循“方案 -> 同意 -> 修改”
